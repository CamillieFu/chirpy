<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
      <%# make it go to the right side %>
        <li class="nav-item dropdown">
          <%= image_tag "chirpylogo.jpg", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <a href="/dashboards" class="active nav-link" onclick="toggleActive()">Dashboard</a>
            <a href="/articles" class="nav-link" onclick="toggleActive()">Articles</a>
            <a href="#" class="nav-link" onclick="toggleActive()">Support</a>
            <%# Need some help styling this with the link_to + icon  %>
            <%# <a href="/users/sign_out" class="nav-link" onclick="toggleActive()"><i class="fa fa-light fa-arrow-right mr-2 dropdown-item"></i>Sign Out</a> %>
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "dropdown-item nav-link sign-out" %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
